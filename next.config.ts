import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['bilder3.n-tv.de','ml.globenewswire.com' ,'media.zenfs.com'], // Add the hostname of your image source
  },

};

export default nextConfig;
