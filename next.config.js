/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'bilder3.n-tv.de',
      'ml.globenewswire.com',
      'media.zenfs.com'
    ]
  }
};

module.exports = nextConfig;